services:

  devops-dashboard:

    container_name: devops-dashboard-pro

    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "8081:8080"

    environment:
      NODE_ENV: production

    restart: unless-stopped
